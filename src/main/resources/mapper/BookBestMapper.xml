<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mytest.oplib.dao.BookBestMapper">

    <!-- 테이블 삽입 -->
    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="bookId">
        INSERT INTO book_best (
            book_rank,
            title,
            author,
            lib_name,
            image,
            publish_year,
            fetched_at)
        VALUES
            <foreach collection="list" item="row" separator=",">
                (#{row.bookRank},
                #{row.title},
                #{row.author},
                #{row.libName},
                #{row.image},
                #{row.publishYear},
                #{row.fetchedAt})
            </foreach>
    </insert>

    <delete id="deleteAll">
        DELETE FROM book_best
    </delete>

    // 페이지 조회
    <select id="selectPage" resultType="com.mytest.oplib.dto.BookBestRow">
        SELECT
            book_id AS bookId,
            book_rank AS bookRank,
            title,
            author,
            lib_name AS libName,
            fetched_at AS fetchedAt
        FROM book_best
        ORDER BY
            <choose>
                <when test="orderBy == 'PUBLISH_YEAR_DESC'">publish_year DESC, book_rank ASC</when>
                <when test="orderBy == 'PUBLISH_YEAR_ASC'">publish_year ASC, book_rank ASC</when>
                <otherwise>book_rank ASC</otherwise>
            </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM book_best
    </select>

</mapper>
